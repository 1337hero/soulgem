# --- game-derived assets: extracted/converted Bethesda content, never publish ---
characters/*/textures/
characters/*/*.glb
body.glb
lydia.html
texcache/
anims/*
!anims/.gitkeep
tts_samples/

# --- souls are personal (persona, voice, memory) — only Aster, the example, ships ---
souls/*
!souls/aster/
souls/aster/memory/*
souls/aster/voice_ref.wav
!souls/aster/memory/.gitkeep

# --- vendored third-party ---
# Rhubarb Lip Sync binary distribution (download: github.com/DanielSWolf/rhubarb-lip-sync)
server/rhubarb/
# niftools NIF format spec, reference only (github.com/niftools/nifxml)
nif.xml

# --- build artifacts ---
bundle.js
node_modules/
bin/
__pycache__/
*.pyc

# --- debris ---
gpucore.*
nohup.out
